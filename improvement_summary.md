# AI-Vtuber プロジェクト改修案

## はじめに

このドキュメントは、GitHubリポジトリ `yuya-fujita-1201/AI-Vtuber` のソースコードを分析し、その結果に基づいて提案する改修案をまとめたものです。本プロジェクトは、TypeScriptをベースとしたモジュール性の高い設計、豊富な機能連携（VTube Studio, OBS）、そして記憶や感情といった高度な概念を取り入れた野心的な試みであり、非常に高いポテンシャルを秘めていると評価できます。

提案は、**「パフォーマンスと安定性の向上」「会話品質と人間らしさの向上」「機能拡張とエンターテイメント性の向上」** の3つの柱で構成されています。既存の優れた設計思想を活かしつつ、ドキュメントに記載されている課題（`feedback_ja.md`など）を解決し、プロジェクトをさらに発展させることを目的としています。

## 1. パフォーマンスと安定性の向上

アプリケーションの基盤を強化し、より安定したライブ配信を実現するための改善案です。

### 1.1. コメント処理の非同期化とスロットリング

- **現状の課題**: `feedback_ja.md` にある通り、全てのコメントを処理しようとすることで、コメントが集中した場合（いわゆる「弾幕」状態）に処理が追いつかず、遅延が発生する「全レス問題」が指摘されています。
- **改修案**: Agentのメインループ(`tick`)で取得したコメントを一旦キューに格納し、別の非同期ワーカーが一定間隔（例: 2秒に1件）でキューからコメントを取り出し処理する方式（スロットリング）を導入します。これにより、コメントの量に左右されず、安定した応答間隔を維持できます。
- **実装**: `Agent.ts`内にコメントキュー（`pendingComments`とは別の処理待ちキュー）を設け、`processQueue`とは独立したコメント処理ループを追加します。

### 1.2. LLMリクエストのタイムアウトとリトライ処理の強化

- **現状の課題**: `OpenAIService.ts`などを見ると、LLMへのリクエストが失敗した場合、フォールバックテキストを返すのみで、リトライ処理がありません。一時的なネットワークエラーで応答が途切れる可能性があります。
- **改修案**: LLMリクエストに明確なタイムアウト（例: 10秒）を設定し、タイムアウトまたは特定のエラー（例: 5xx系エラー）が発生した場合、指数バックオフを用いたリトライ処理（例: 3回まで）を実装します。これにより、システムの堅牢性が向上します。
- **実装**: `OpenAIService.ts`や`GroqService.ts`の`generateText`メソッド内に、`axios-retry`のようなライブラリの概念を導入するか、手動でリトライロジックを実装します。

### 1.3. 設定管理の動的読み込みと検証

- **現状の課題**: `AppConfig.ts`は起動時に環境変数を読み込みますが、起動後の変更は反映されません。また、必須の設定値（例: `OPENAI_API_KEY`）が不足している場合のエラーハンドリングがサービスごとに行われています。
- **改修案**: アプリケーション起動時に設定の網羅的な検証（バリデーション）を行う処理を追加し、必須項目が欠けている場合はエラーメッセージを出して起動を中止させます。また、特定のAPI（例: `/reload-config`）を介して、アプリケーションを再起動せずに一部の設定（プロンプトテンプレートや各種パラメータなど）を動的に再読み込みできる仕組みを導入します。
- **実装**: `AppConfig.ts`にZodなどのバリデーションライブラリを導入し、`index.ts`の起動シーケンスで検証処理を呼び出します。

## 2. 会話品質と人間らしさの向上

AI VTuberの「魂」とも言える、より自然で魅力的な会話を実現するための改善案です。

### 2.1. 記憶システムの階層化と活用深化

- **現状の課題**: `MemoryService.ts`はPrismaとChromaDBを組み合わせた高度な記憶システムを実装していますが、`Agent.ts`での活用が主に返信生成時の関連情報検索に留まっています。記憶の揮発性（短期・中期・長期）が明確ではありません。
- **改修案**: 記憶を以下の3階層に分け、それぞれの役割を明確にします。
    1.  **短期記憶（ワーキングメモリ）**: 直近5〜10件の会話履歴。文脈の維持に利用。（現状の`recentComments`に相当）
    2.  **中期記憶（エピソード記憶）**: 現在の配信中における重要な出来事や視聴者との約束。`MemoryService`に`streamId`を紐付けて保存。
    3.  **長期記憶（セマンティック記憶）**: 配信を横断して保持される普遍的な知識や視聴者の個人的な情報（例: 「〇〇さんは猫を飼っている」）。
- **実装**: `Agent.ts`の`consolidateStreamMemory`メソッドを強化し、配信終了時に中期記憶を要約して長期記憶に変換・保存するプロセスを追加します。また、独り言生成時にも関連する記憶を検索・活用するように`maybeGenerateMonologue`を改修します。

### 2.2. 感情・意図理解のLLMへの移行とリッチ化

- **現状の課題**: `EmotionEngine.ts`や`IntentClassifier.ts`はキーワードベースのルールで感情や意図を判定しており、皮肉や複雑なニュアンスの理解が困難です。
- **改修案**: 軽量LLM（例: `gpt-4o-mini`）のFunction CallingやJSONモードを活用し、コメントから「意図（複数選択可）」「感情（Positive/Negative/Neutralのスコア）」「主要トピック」を構造化データとして抽出する`LLMClassifier`を導入します。これにより、`CommentRouter`と`IntentClassifier`を統合・高度化します。
- **実装**: 新たに`services/LLMClassifierService.ts`を作成し、`Agent.ts`から呼び出す形にします。抽出した感情スコアを`EmotionEngine`の`moodScore`に直接反映させることで、よりリッチな感情表現を可能にします。

### 2.3. プロンプトエンジニアリングの動的化

- **現状の課題**: `PromptManager.ts`はテンプレートベースで優れた構造化プロンプトを生成しますが、内容は固定的です。
- **改修案**: `EmotionEngine`が判定した現在の感情状態（`HAPPY`, `SAD`など）や、`StorytellingService`が管理する会話の雰囲気（`vibe`）に応じて、システムプロンプトの指示（例: 「あなたは今、少し悲しい気持ちです。落ち着いたトーンで話してください」）を動的に変更します。これにより、AIの応答が状況に応じて変化し、より人間らしい「ゆらぎ」が生まれます。
- **実装**: `PromptManager.ts`の`buildStructuredSystemPrompt`メソッドに、現在の感情状態や会話の雰囲気を引数として渡し、プロンプトの末尾に動的な指示を追加するロジックを組み込みます。

## 3. 機能拡張とエンターテイメント性の向上

視聴者とのインタラクションを深め、配信をより楽しくするための新機能提案です。

### 3.1. ダッシュボード機能の強化と双方向化

- **現状の課題**: `public/dashboard.html`はイベントログを表示するシンプルなダッシュボードですが、ここからAIを操作することはできません。
- **改修案**: ダッシュボードに以下の機能を追加し、配信者がリアルタイムでAIをコントロールできるようにします。
    1.  **感情の手動オーバーライド**: 特定の感情（例: `EXCITED`）を一時的に強制するボタンを設置。
    2.  **独り言の強制トリガー**: ボタン一つでAIに独り言を話させ、会話のきっかけを作る。
    3.  **NGワード・ミュート機能**: 不適切な単語や特定のユーザーを一時的に無視する機能。
- **実装**: `WebServer.ts`でSocket.IOの双方向通信を整備し、ダッシュボードからのイベント（例: `force_emotion`）を待ち受け、`Agent.ts`の対応するメソッド（例: `agent.emotionEngine.lockState()`）を呼び出すようにします。

### 3.2. 外部情報ソースとの連携

- **現状の課題**: AIの知識はLLMが持つ内部情報に限定されており、リアルタイムの情報（ニュース、天気など）に言及できません。
- **改修案**: 特定のキーワード（例: 「今日のニュースは？」）に反応して、外部API（News APIなど）を叩き、取得した情報を基にLLMが応答を生成する機能を導入します。これにより、時事ネタなどリアルタイム性の高い話題に対応できるようになります。
- **実装**: `Agent.ts`の`tick`処理内にコマンドハンドラを追加し、特定のキーワードを検知したら、新たに作成する`services/NewsApiService.ts`などを呼び出し、その結果をプロンプトに含めてLLMに渡します。

### 3.3. テストとデバッグ機能の拡充

- **現状の課題**: `FileReplayAdapter`によるテストはありますが、特定の状況を再現するのが難しい場合があります。
- **改修案**: `debug_youtube.ts`を拡張し、特定のコメントシーケンスやイベント（例: 大量のコメント、特定のユーザーからの質問）をシナリオファイル（JSON形式）として定義し、それを再生してエージェントの挙動をテストできるシナリオテスト機能を強化します。また、各モジュールの単体テスト（Jestなどを使用）を導入し、コードの品質と保守性を向上させます。
- **実装**: `scripts`ディレクトリにテスト用のスクリプトを追加し、`package.json`に`test`コマンドを定義します。Jestなどのテストフレームワークを導入し、主要なサービスクラス（`EmotionEngine`, `MemoryService`など）からテストを作成していきます。

## まとめ

本改修案は、既存の優れたアーキテクチャを尊重しつつ、パフォーマンスの安定化、対話品質の向上、そしてエンターテイメント性の強化を目指すものです。特に、コメント処理の非同期化、記憶システムの階層化、そしてLLMによる意図・感情理解の高度化は、本プロジェクトを次のステージへ引き上げるための重要な鍵となると考えます。
